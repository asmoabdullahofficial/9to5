---
layout: null
---

[
  {% for post in site.posts %}
  {
    "id": "{{ post.id }}",
    "title": "{{ post.title | escape }}",
    "url": "{{ site.baseurl }}{{ post.url }}",
    "content": "{{ post.content | strip_html | escape | truncate: 3000 }}"
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
